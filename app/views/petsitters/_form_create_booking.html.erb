<%= simple_form_for [@petsitter, @booking] do |f| %>

  <%= f.input :start_date %>
  <%= f.input :end_date %>
  <%= f.input :location, as: :boolean, label: 'Do you want the petsitter to keep your pet at his home ?' %>

  <%= f.submit %>

<% end %>
